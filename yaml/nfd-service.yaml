apiVersion: v1
kind: Service
metadata:
  name: nfd-service
spec:
  selector:
    app: nfd # ★ NFDのPodに付いているラベルを指定
  ports:
    - name: ndn-tcp
      protocol: TCP
      port: 6363
      targetPort: 6363
